import 'package:flutter/material.dart';

class AppColors {
  // Layout Colors
  static const Color background = Color(0xFFF8FAFC);
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE2E8F0);

  // Sidebar Colors
  static const Color sidebar = Color(0xFF0F172A);
  static const Color sidebarHover = Color(0xFF1E293B);
  static const Color sidebarActive = Color(0xFF2563EB);

  // Theme/Action Colors
  static const Color primary = Color(0xFF2563EB); // Primary Button
  static const Color primaryHover = Color(0xFF1D4ED8); // Button Hover
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFDC2626); // Error

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);

  // Backwards compatibility for existing code
  static const Color primaryAccent = Color(0xFF2563EB);
  static const Color error = Color(0xFFDC2626);
}
