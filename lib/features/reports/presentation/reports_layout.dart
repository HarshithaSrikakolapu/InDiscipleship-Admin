import 'package:flutter/material.dart';

class ReportsLayout extends StatelessWidget {
  final Widget child;

  const ReportsLayout({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
